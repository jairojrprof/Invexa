{
  "name": "invexa",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:db": "node --test tests/database/*.test.mjs"
  },
  "devDependencies": {
    "pg": "8.16.3"
  }
}
